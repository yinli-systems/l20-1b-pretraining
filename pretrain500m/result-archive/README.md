# 529M result archive (2026-09-15 UTC)

This directory adds the small ParaCloud outputs that were not yet in GitHub. The existing `pretrain500m/reports/` tree remains the primary summary and receipt collection. `para-20260915/` preserves remote paths under `/ssd/scxi253/pretrain500m-20260912-v1` for training metrics, evaluations, manifests, checksums, and success and failure logs. `manifest-20260915.json` records every copied file's byte length and SHA-256; all 1,247 retained remote files matched an independent SHA-256 reread on ParaCloud after transfer. Run `cd para-20260915 && shasum -a 256 -c SHA256SUMS` to check the local snapshot.

Two source-side `resume-comparison.json` files are zero bytes and are retained as empty negative artifacts. All other copied `.json` files parse successfully.

`local-sanitized/` publishes the complete per-candidate quality metadata, the 60 sampled document identities, and human review decisions from the formerly ignored local and remote reports. The three source-excerpt fields (`start`, `middle`, `end`) are omitted from each candidate or sample because this repository is public. Each sanitized report records the original file's byte length and SHA-256.

`remote-large-and-sample-artifacts-index-20260915.json` inventories 124 remote checkpoint/model-shard and per-example benchmark sample files totaling 43.7 GB. The large binary models and benchmark-question sample dumps remain on ParaCloud; 16 checkpoint identities are recorded from remote checksum sidecars, which were copied but not independently rehashed in this archive. No model binary or benchmark sample dump is committed here.

The established F2 two-seed screen and seven-task comparison are in [`../reports/f2-two-seed-completed-20260914.json`](../reports/f2-two-seed-completed-20260914.json) and [`../reports/seven-task-confirmation-2gpu-results-1590907.json`](../reports/seven-task-confirmation-2gpu-results-1590907.json). The hash-bound 0.537B-token-per-seed F2 continuation input and submission receipts are in the `../reports/f2-continuation-*.json` files. Jobs 1592301 and 1592302 were submitted for one node with four public RTX 5090 GPUs each, but at [22:12 UTC on 2026-09-15](f2-continuation-status-20260915T2212Z.json) they were still pending with no allocation or new training result. Historic MFU and English seven-task accuracy do not establish a 50%+ score or superiority over other models.

Both jobs subsequently started on separate four-5090 nodes. The [23:43 UTC live receipt](f2-continuation-live-start-20260915T2340Z.json) records exact job allocation, GPU UUIDs, pinned input identities, observed steps, finite losses, and the last-ten-step MFU gate. It is an interim progress snapshot; completion, checkpoints, and independent quality evaluation remain pending.
