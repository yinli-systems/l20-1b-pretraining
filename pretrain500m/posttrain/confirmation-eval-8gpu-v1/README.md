# Eight-GPU held-out confirmation launcher

Runs the immutable confirmation-eval-v1 source on two four-GPU nodes with
microbatch 2, preserving the same exact 16-block global validation batch. Use
when four complete nodes are unavailable; GPU count changes throughput only.
